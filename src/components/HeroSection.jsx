import React from 'react';
import './HeroSection.css';

const HeroSection = () => {
    return (
        <section className="hero-section">
            <div className="hero-content">
                <h2 className="hero-title">Welcome to the Ultimate Movie Review Destination</h2>
                <p className="hero-subtitle">Find reviews for the latest and greatest movies.</p>
            </div>
        </section>
    );
};

export default HeroSection;
